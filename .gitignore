node_modules/
backend/data.db
*.log
dist/
.DS_Store

# Environment secrets — never commit
.env
.env.local
.env.*.local
# frontend/.env.production is committed (contains only public CloudFront URL, no secrets)
